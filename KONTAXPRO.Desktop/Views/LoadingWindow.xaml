<Window x:Class="KONTAXPRO.Desktop.Views.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding LoadingTitle}"
        Width="620"
        Height="360"
        MinWidth="500"
        MinHeight="320"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False">

    <Grid Background="{DynamicResource OverlayBrush}">
        <Border Style="{StaticResource KontaxDialogCardStyle}"
                Width="440"
                Padding="34,30"
                Margin="28"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center">
                    <TextBlock Text="KONTAX"
                               Foreground="{DynamicResource BrandKontaxBrush}"
                               FontSize="25"
                               FontWeight="Bold"/>
                    <TextBlock Text="PRO"
                               Foreground="{DynamicResource BrandProBrush}"
                               FontSize="25"
                               FontWeight="Bold"/>
                </StackPanel>

                <Grid Width="62"
                      Height="62"
                      Margin="0,25,0,22"
                      HorizontalAlignment="Center">
                    <Ellipse Stroke="{DynamicResource BorderBrush}"
                             StrokeThickness="5"/>
                    <Ellipse Stroke="{DynamicResource BrandProBrush}"
                             StrokeThickness="5"
                             StrokeDashArray="3,6"
                             StrokeDashCap="Round"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotation"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="SpinnerRotation"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="0"
                                                         To="360"
                                                         Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>

                <TextBlock Text="{Binding LoadingTitle}"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           FontSize="18"
                           FontWeight="Bold"
                           TextAlignment="Center"
                           TextWrapping="Wrap"/>

                <TextBlock Text="{Binding Message}"
                           Margin="0,9,0,0"
                           Foreground="{DynamicResource TextMutedBrush}"
                           FontSize="13"
                           LineHeight="19"
                           TextAlignment="Center"
                           TextWrapping="Wrap"/>

                <TextBlock Text="Por favor, espere…"
                           Margin="0,15,0,0"
                           Foreground="{DynamicResource BrandProBrush}"
                           FontSize="11"
                           FontWeight="SemiBold"
                           TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

